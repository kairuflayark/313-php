function get_list (ship, location)
{
    var location = getElementbyID()
    var loadU
    var xmlhttp = new XMLHttpRequest();
    xmlhttp.onreadystatechange = function() {
        if (this.readyState == 4 && this.status == 200) {
    
        }
    };
    xmlhttp.open("POST", "", true);
    xmlhttp.send();
}